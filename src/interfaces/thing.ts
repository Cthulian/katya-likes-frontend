export default interface Thing {
  id: number
  attributes: {
    title: string
    description: string
    interest: string
    createdAt: string
    updatedAt: string
    publishedAt: string
    backgroundColor: string
  }
}
